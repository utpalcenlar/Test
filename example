from azure.core.credentials import AzureKeyCredential
# --- B. PROCESS VIA AI CONTENT UNDERSTANDING ---
        ai_key = os.environ["AZURE_AI_KEY"]
        cu_client = ContentUnderstandingClient(endpoint=ai_endpoint, credential=AzureKeyCredential(ai_key), api_version=api_version)
